version: "2"
run:
  timeout: 5m

linters:
  default: standard
  exclusions:
    paths:
      - restapi/internal/api  # generated by oapi-codegen

formatters:
  enable:
    - gofmt
  settings:
    gofmt:
      simplify: true
