#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;
    while(true) {  // TLE (Time Limit Exceeded)
        // infinite loop
    }
    cout << a + b << endl;
    return 0;
}