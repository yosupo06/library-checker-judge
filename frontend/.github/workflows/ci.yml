name: CI

on:
  push:
    branches: 
      - master
  pull_request:

jobs:
  prettier-eslint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: '14'
      - name: install
        run: |
          npm install
      - name: lint
        run: |
          npm run lint
